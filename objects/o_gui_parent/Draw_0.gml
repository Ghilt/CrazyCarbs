// Do nothing

/* A gui object is managed by tge gui manager. It manages the elements position when transitioning through states.
 *
 * TODO remove that manager, and put the functionali9ty in this parent object instead
 *
 * A gui object need to define the following:
 * 
 * mov - managing the current movement
 * battlePos - position when in battle state
 * buildPos - positino when in build/explore phase 
 *
 */ 
