// Do nothing

/* A gui object is managed by the gui manager. It manages the elements position when transitioning through states.
 *
 * A gui object need to define the following:
 * 
 * mov - managing the current movement
 * battlePos - position when in battle state
 * buildPos - positino when in build/explore phase 
 *
 */ 
